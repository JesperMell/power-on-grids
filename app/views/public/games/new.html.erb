<%= render 'public/shared/head' %>
<br>
<br>
<br>
<br>
<br>
<div class="container">
  <%= form_for [:public, @game] do |f| %>
    <legend>Nytt Spel</legend>
  <div class="form-group">
    <label class="control-label">Namn</label>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <label class="control-label">Karta</label>
    <%= f.select :map_id, Map.all.map{|m| [m.name, m.id]}, {}, {class: 'form-control'} %>
  </div>
  <div class="form-group">
    <label class="control-label">Spelare</label>
    <div class="row">
      <div class="col-sm-10">
        <%= select "users", "users", User.all.map{|u|[u.email, u.id]}, {}, {id: 'users', class: 'form-control'} %>
      </div>
      <div class="col-sm-2">
        <a id="btn-add-player" class='btn btn-success'>Lägg till</a>
      </div>
    </div>
    <table id="player-table" class="table">
      <thead>
        <tr>
          <th>Spelare</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody>
      </tbody>
    </table>
  </div>
  <div class="form-group">
    <button class="btn btn-primary">Starta</button>
  </div>
<% end %>
</div>
